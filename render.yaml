services:
  - type: web
    name: postmortem-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn api.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: LLM_PROVIDER
        value: huggingface
      - key: HF_TOKEN
        sync: false
